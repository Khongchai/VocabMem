import { Box } from "@chakra-ui/react";
import React from "react";

interface logoutProps {}

export const logout: React.FC<logoutProps> = ({}) => {
  return <Box>Logout Page</Box>;
};

export default logout;
